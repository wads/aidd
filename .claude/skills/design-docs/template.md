# 設計書テンプレート

ファイル名: `{records_root}/design/{連番}-short-title.md`（例: `0007-saved-search.md`。連番 4 桁 0 埋め、service/system 分割は Binding に従う）

```md
# 設計: {タイトル}

- Issue: {Issue 番号}
- 関連 ADR: {リンク。なければ「なし」}
- Replaces: {置き換える旧設計書。なければ「なし」}

## 目的 / 非目的

{この設計で達成すること。意図的にやらないこと}

## 技術選定との対応

{関連 ADR の決定が、この設計のどの構造・制約として現れるか。ADR が無ければ節ごと削る} 🔵/🟡

## 構造図

```mermaid
{関係する範囲のレイヤー図またはコンポーネント図（必須）。クラス図・シーケンス図は設計要素があるときに追加}
```

## 責務分割

{どのコンポーネント/モジュールが何を担当するか。なぜこの分割か。依存の方向} 🔵/🟡

## 命名とパターン

{採用する命名規則・適用するパターン（レイヤー / リポジトリ / 戦略 など）と、既存コードとの整合。逸脱があればその理由} 🔵/🟡

## データと状態

{扱うデータ、状態の持ち方、ライフサイクル} 🔵/🟡

## 主要フロー

{正常系の流れ。図が必要なら簡潔に}

## 例外ケース

{エラー、空、境界、競合時の振る舞い}

## 守るべき振る舞い

{将来の変更者が知らないと壊す不変条件。テストで表現できるものはテスト名を併記}

## 技術的負債の方針

{意図的に受け入れる負債と、その返済条件（いつ・何が起きたら返すか）。増やさないための制約（例: 層をまたぐ直接参照を禁止）。受け入れる負債が無ければ「なし」と明記（節は削らない）}

## AC 対応

| AC-ID | 満たす設計要素 |
|---|---|
| AC-1 | {セクション・コンポーネント} |

## 未確定事項

{残っている未決と、誰がいつ決めるか}
```

ルール:

- 各記述に信頼性マーカー（🔵 根拠あり / 🟡 推定 / 🔴 要確認）を付ける。承認時に 🟡・🔴 が残っていてはならない
- 書くべき内容がない節は「特になし」ではなく節ごと削る（例外: 「構造図」と「技術的負債の方針」は必須。負債が無い場合は「なし」と明記する）
- 旧設計書は書き換えない。変更は新しい設計書 + Replaces で表す
