---
name: stacked-pr
description: stacked PR（積み上げ式 PR）で進めるときのプロセス skill。複数ステップの計画を ステップ = 1 PR で積み、PR 単位でゲート・人間チェック・マージを回す。git / GitHub の機構は GitHub ネイティブ機能（`gh stack`）に委ね、この skill はプロセスの規則だけを持つ。
user-invocable: true
---

# Stacked PR

## いつ使うか

- P5 実装計画でスタック構成に合意した Issue の実装を進めるとき（適用条件は `implementation-plan/workflow.md` §3・`dev/routes.md`）
- スタックにしない計画（ステップが 1 つ、各ステップが独立にマージできない、P5 を通らないルート、bugfix の簡易計画）では使わない

## 環境要件

- **gh 2.90.0 以降**と `github/gh-stack` 拡張（`gh extension install github/gh-stack`）
- git 2.20 以降
- 同一リポジトリ内で完結すること（クロスフォークのスタックは非対応）
- **GitHub のパブリックプレビュー機能**であり、仕様が変わる可能性がある

**前提を満たさない環境で、その計画にスタックの有用性がある場合は、人間に確認する**。

- 選択肢は「前提を満たす（gh の更新・拡張の導入）」か「従来の 1 PR で進める」か
- **AI が単独で 1 PR へ落とさない**。前提は解消できることが多く、黙って落とすとスタックの利点を失う判断を人間が知らないまま行うことになる
- 前提を満たせないことが確定したら、従来の 1 PR で進める。生の git コマンドで機構を代替する手順は持たない

## 入力

- スタック構成（P5 実装計画で合意したステップ = 1 PR の境界・順序。Issue コメントの計画全体が正）

## 出力

- ステップごとの PR（base の連鎖は `gh stack` が管理する）
- 各 PR のゲート結果・検証結果・チェック依頼 → PR コメント

## 扱う Intent

- Execution Intent（実行。計画の正は Issue コメント側）

## 原則

- **機構は `gh stack` に委ねる**。ブランチの積み替え・base の付け替え・マージ順序の制御を生の git / `gh pr` コマンドで自前に行わない（両者が同じ状態を別々に管理すると壊れる）
- 各 PR の差分は自ステップ分のみを示す
- マージ順序（最下段から）は**サーバー側が強制する**。ミッドスタックの単独マージはできない
- **積み替えを行った PR は、差分が変わったかによらず「ゲート通過済み・人間チェック済み」が失効する**（ADR-0006）。失効は積み替えの直後、再ゲートは次にその PR を人間チェックへ出す直前
- コンフリクトの解消は、自明なら AI が行い、意味の選択が生じるなら人間に確認する

## 想定外のケースに出会ったとき

この skill は `gh stack` の全機能を網羅していない。**手順書に無い状況**（未知のエラー、コマンドの挙動が記述と違う、扱い方の分からない操作）に出会ったら:

1. **推測で回避せず、作業を止めて人間に相談する**
2. 公式ドキュメントを確認して対応を決める
3. 繰り返し起きるものだけをこの skill へ追記する

この Issue では GitHub の挙動を推測で記述した結果、**実測でしか分からない誤りが 3 回混入した**（自動ブランチ削除の警告が逆、rename API で PR が閉じる、diff 不変判定がバイナリを原理的に判定できない）。網羅的に書こうとするより、出会った時点で確認する方が確実である。

## 読むべき補助ファイル

- `workflow.md`
