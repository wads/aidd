# Examples

```text
/tdd-cycle 商品一覧 API にページネーションを追加する
```

```text
/tdd-cycle 検索フォームで未入力時にバリデーションエラーを返す
```

## 1 サイクルの実演（AC-2: 保存済み条件がないユーザーには初期状態の検索画面が表示される）

```text
TODO リスト:
- [ ] AC-1: 保存した検索条件が次回アクセス時に復元される
- [x] AC-2: 保存済み条件がないユーザーには初期状態が表示される ← いまここ
- [ ] AC-3: 保存に失敗した場合エラーメッセージが表示される

Red:
  test("保存済み条件がないユーザーには初期状態の検索画面を返す", ...)
  → 実行: FAIL（restoreSearchCondition が undefined を返し初期値が適用されない）
  → 期待どおりの理由で失敗。Red 成立

Green（明白な実装）:
  restoreSearchCondition が null のとき DEFAULT_CONDITION を返す分岐を追加
  → 実行: PASS

Refactor:
  デフォルト値の定義が 2 箇所に重複していたため DEFAULT_CONDITION に集約
  → 実行: 全テスト PASS のまま

対応表を PR 説明へ追記:
  | AC-2 | search-condition.test.ts > 保存済み条件がないユーザーには初期状態の検索画面を返す |
```

## 停止の例（予期せぬ Green）

```text
AC-3 のテストを書いて実行したところ、一度も赤にならず PASS した。
実装前のため、テストが何も検証していないか、既存実装が既にこの振る舞いを持つ可能性がある。
作業を停止して確認します: 既存の error handler が同等の表示を行っているようです。
AC-3 を「既存挙動の確認のみ」に変更してよいか判断をお願いします。
```
