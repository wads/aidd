# 設計叩き台: dev skill のフェーズ分解とコンテキスト契約

- Status: Draft（叩き台・議論用）
- Date: 2026-06-11
- 関連: [intent-driven-development.md](intent-driven-development.md), [ADR-0001](../adr/0001-restructure-skills-for-codex-and-claude.md)

## 目的

Issue 作成（`product-intent`）後の開発プロセスを工程（フェーズ）ごとに分解し、各フェーズについて以下を契約として定義する。

- AI エージェントと人間がそれぞれ何を行い、何を議論するか
- どの成果物・コードを作成するか
- どの品質・受入れ基準（exit 条件）で次フェーズへ進むか
- どのコンテキストを、どこに、どの寿命で残すか

この文書の合意後、各フェーズを skill として実装する。

## 非目的

- 全体を統括する orchestrator の設計（別途作成予定）
- 各 skill 本文の実装（この文書はその前段の合意形成。`product-intent` のタイプ対応改修を含む）
- worktree・branch の物理的な管理方法（`workspace-hygiene` と実行環境の責務。本プロセスは P1 着手の entry 条件として「クリーンな作業ツリー」を要求するだけで、その実現手段には関与しない）

## 前提（確認済み）

- 成果物の実体はプロダクト repo の `docs/` 配下に置く。playbook 側はテンプレートとルールのみを持つ
- スコープはIssue 作成後から retrospective まで
- 短期コンテキスト（実装方針・検証メモ等）は PR/Issue に残すだけでよい。repo に永続化するのは ADR・設計書
- 実行主体の AI エージェントは当面 Claude を想定するが、本文書の表記は特定エージェントに依存しない（以下「AI」と表記。Codex 等の他エージェントにも適用できる）

## 議論と決定の原則

本プロセスおよび派生するすべての skill の議論パートに適用する。

- AI は各フェーズの議論で、率直な意見・懸念・反論を提示する。人間の意向への同調や忖度を品質より優先しない
- 率直さは訓示ではなく**出力契約**として要求する: 承認を求める提示には「AI の見解」（推奨 / 棄却した代替案 / 懸念）を必須フィールドとして含める。書式・運用ルールは `common.md` を正とする
- AI は人間の意向を聞く前に自身の評価を提示する（意向へのアンカリングを避けるため。`multi-agent-discussion` がエージェント間で使う手法の人間対話への適用）
- 最終決定は常に人間が行う。ゲートの承認を AI が代行しない
- 意見が対立したまま決定された場合も決定に従う。ただし採用されなかった懸念は、ADR の「採用しなかった選択肢」や Intent Delta として記録してよい

## コンテキストの 2 層構造

intent-driven-development.md の Intent 分類を、寿命と置き場所で 2 層に整理する。

### 長期層（プロダクト repo、イミュータブル）

「なぜこの形なのか」を将来の AI と人間が復元するための記録。上書きせず、変更は新しい文書または Intent Delta で表す。

| 成果物 | Intent | 置き場所 |
|---|---|---|
| ADR | Decision Intent | `docs/adr/{連番}-short-title.md`（例: `0042-cache-strategy.md`。Issue 番号は frontmatter またはIssue コメントで紐づける。`adr/workflow.md`） |
| 設計書 | Design Intent | `docs/design/{連番}-short-title.md` |
| テストコード | Verification Intent | テストディレクトリ（コードとして永続） |

### 短期層（PR / GitHub Issue、使い捨て）

該当タスクを正しく完了させるための作業コンテキスト。マージ・クローズ後はメンテナンスしない（検索可能なら十分）。

| 成果物 | Intent | 置き場所 |
|---|---|---|
| 確定版受入れ条件・品質条件 | Product Intent への Intent Delta | Issue コメント |
| プロトタイプ一式・`IMPLEMENTATION_PROMPT.md` | Design Intent（UI） | マージしない proto branch（GitHub Issue からリンク） |
| 実装方針 | Execution Intent | draft PR の説明文 |
| 受入れ条件⇄テスト対応表 | Verification Intent | PR 説明文 |
| 検証メモ（手順と結果） | Verification Intent | PR コメント |
| コンテキストスナップショット（HTML） | 全 Intent の生成ビュー | GitHub Issue添付（再生成可能・保守しない） |
| 振り返り | Learning Intent | Issue コメント（採用された改善は playbook への PR） |

### 短期コンテキストの昇格ルール

短期層の内容は原則使い捨てだが、P7 検証・レビュー（検証・レビュー）と P8 振り返り（振り返り）で次を判定し、該当すればマージ/クローズ前に長期層へ昇格（短期コンテキストの昇格）させる。

- 将来の変更時に壊しやすい「守るべき振る舞い」が短期層にしか書かれていない → 設計書へ
- 選択肢を比較して捨てた判断が短期層にしか書かれていない → ADR へ

判定は AI が提案し、人間が承認する。

### 相互リンク規約

索引は GitHub Issue が担うため、全成果物に Issue 番号（`#{番号}` 形式。`#123`、`#11`、`#4356` など）を機械的に埋め込む。本規約の例では `#123` を使うが、特定のリポジトリ・プロジェクトには依存しない。Issue を別リポジトリ（コンテキストハブ）へ集約する場合は Binding の `issue_repo` を起点に参照する。

- ADR のファイル名: `{連番}-short-title.md`（例: `0042-cache-strategy.md`）。連番は 4 桁 0 埋めで ADR ディレクトリ内で独立。Issue 番号は frontmatter またはIssue コメントで紐づけ、ファイル名には含めない（`adr/workflow.md`）。設計書は `design-docs` skill の規約に従う
- 同一Issue から複数の ADR が出る場合は連番と short-title で区別する
- short-title は小文字ケバブケースとする
- 並行ブランチで連番が衝突した場合は、後にマージする側がリネームする（P7 検証・レビューのチェック項目に連番重複の検出を含める）
- branch 名: `feat/#123-short-desc`
- PR タイトル: `#123: 変更の要約`
- Issue には ADR・設計書へのリンクをコメントで貼る

連番はディレクトリ内の並び順（読む順序のヒント）のためのもの。ADR の新旧・置き換え関係の正は frontmatter の関係リンク（`supersedes` / `superseded_by`、補足は `amends` / `amended_by`。必ず双方向）とし、有効な ADR の一覧は ADR ディレクトリの `INDEX.md`（生成物）で引く（ADR-0003）。設計書の置き換えは `design-docs` skill の「Replaces」表記に従う。

### PR 説明の必須セクション

PR 説明には以下のセクションを必須とする。該当がない場合も省略せず「なし」と明記する（暗黙の省略を構造的に不可能にするため）。

- 受入れ条件⇄テスト対応表（AC-ID で参照）
- 技術判断: あり（ADR へのリンク）/ なし
- 短期コンテキストの昇格判定: あり（内容）/ なし
- 未検証項目: あり（内容と理由）/ なし

既存の PR テンプレートを持つプロダクトでは、テンプレートを置き換えず必須セクションを既存テンプレートへ組み込む。組み込み方はプロダクトごとの preset で吸収する。

## フェーズ全体像

```text
product-intent（済）
  → P1 着手 → P2 要求整理 ⇄ P2.5 プロトタイピング（UI 変更時のみ）
  → P3 技術判断 ⇄ P4 設計 → P5 実装計画
  → P6 実装 → P7 検証・レビュー → P8 振り返り
```

P3 技術判断と P4 設計は反復可能とする（設計の途中で新しい技術判断が生まれることは普通にある）。P2 要求整理と P2.5 プロトタイピングも同様に反復する（プロトタイプの合意が受入れ条件を変える）。

### 受入れ条件・品質条件のトレース

受入れ条件・品質条件は次の経路でコードに到達し、2 段のゲートで検証される。

1. P2 要求整理で確定し、ID（`AC-n` / `QC-n`）を振る（Issue コメント）。P2.5 プロトタイピングを実施した場合は UI の合意を Delta として反映する
2. P5 実装計画で各実装ステップに対応付ける（PR 説明）
3. P6 実装で `tdd-cycle` によりテストへ変換する（受入れ条件⇄テスト対応表）。設計書の「守るべき振る舞い」も同様にテスト化する
4. **ゲート 1（P6 実装 exit、主に機械）**: 全 AC に対応するテストが green（対応の完全性は AC-ID で機械チェック可能）、lint・型・カバレッジ等の機械チェック可能な品質条件を満たす（検証手段タグが CI・テストの項目）
5. **ゲート 2（P7 検証・レビュー、人間 + AI）**: 人間レビューの前に `critical-gate`（独立視点の批判的レビュー。Critical は戻り先フェーズへ差し戻し、解消まで通過させない）を通す。その後、テストで表現できない条件（見た目・操作感・実機確認）の検証（検証手段タグが手動の項目）、diff と ADR・設計書の整合確認、人間レビューと受入れ判断。P2 要求整理の確定前・P5 承認前にも `critical-gate` の軽量版を推奨する

### フェーズ間の戻り（反復）

フェーズは一方向に流れるとは限らない。後段の議論・実装で前段の検討漏れや前提の崩れに気づくのは、反復を前提とした本プロセスの**正常な挙動**であり、事故ではない。ゲートで条件を満たせないと判明した場合も、議論の中で検討漏れに気づいた場合も、その場で作り込まず、原因に応じたフェーズへ戻る。

| 戻りの原因 | 戻り先 |
|---|---|
| 受入れ条件が曖昧・矛盾、テストとして表現できない | P2 要求整理（プロダクト判断が必要なら `product-intent` の確認ループへ） |
| 合意した UI・操作感が実現できない、または合意が崩れた | P2.5 プロトタイピング |
| 技術選定の前提が崩れた | P3 技術判断（既存 ADR の本文は書き換えず、置き換える ADR を追加して双方向リンクで結ぶ） |
| 設計の責務分割・フローが成立しない | P4 設計 |
| 実装順序・粒度だけの問題 | P5 実装計画 |

戻る際のルール:

1. **Delta を残してから戻る**: 戻る理由を Intent Delta として Issue へ追記する（書式は `common.md`）。サイレントに後段で辻褄を合わせない
2. **部分再入**: 戻った先はフルにやり直さない。変わった項目（AC-ID / QC-ID / ADR / 設計要素）を参照する下流成果物だけを差分更新する。相互参照（AC-ID）を差分実行の索引に使う
3. **戻りとエスカレーションを区別する**: 検討漏れ・前提変化の発見は「戻り」（AI が Delta を残して再入してよい）。プロダクト判断・スコープ変更・想定外の問題は「エスカレーション」（停止して人間判断を仰ぐ）

| フェーズ | AI の作業 | 人間の作業（承認 = ゲート） | 成果物 → 置き場所 |
|---|---|---|---|
| P1 着手 | 作業ツリー整理、Issue読解、ルート特定 | ルート承認 | branch、ルート判断 → Issue コメント |
| P2 要求整理 | 曖昧さ・エッジケース指摘、品質条件提案 | 不明点回答、品質条件承認 | 確定版受入れ条件・品質条件 → Issue コメント |
| P2.5 プロトタイピング | プロトタイプ生成、PdM とすり合わせ | PdM がプロトタイプ承認 | プロトタイプ → proto branch、UI 条件 Delta → GitHub Issue |
| P3 技術判断 | 選択肢整理、推奨と理由 | 採用判断 | ADR → `docs/adr/` |
| P4 設計 | 責務分割、フロー、例外設計 | 設計承認 | 設計書 → `docs/design/` |
| P5 実装計画 | 実装順序、変更/不変更範囲、検証方針 | 計画承認 | 実装方針 → draft PR 説明 |
| P6 実装 | tdd-cycle、テスト⇄受入れ条件対応付け | 確認ポイント承認、逸脱判断 | コード + テスト、対応表 → PR 説明 |
| P7 検証・レビュー | critical-gate 実行・差し戻し解消、self-review、受入れ検証、昇格提案、スナップショット生成 | PR レビュー、受入れ判断、マージ | ゲート判定・検証メモ → PR コメント、スナップショット → ワークスペースに使い捨て保存（要点 → Issue コメント） |
| P8 振り返り | Keep/Problem の観察提示、人間との突き合わせ、改善提案 | 自身の Problem 提示、Try の採否 | 合意した改善 → playbook PR・プロジェクトのルール/メモリ |

## タイプ別ルート

どのフェーズを実施するかは、変更規模（旧 Lite / Standard / Full）ではなくIssue のタイプで決める。タイプはIssue 作成時（`product-intent`）の必須入力とし、GitHub ラベル（`type-*`）として付与する。P1 着手はラベルを読むだけでルートが決まる。

規模・リスクによる深さの調整は、ルールの軸を増やすのではなく、判断駆動の成果物作成（ADR・設計書は判断が生じたときだけ）と escalation（想定外が出たら停止して人間判断）が担う。**タイプ × 規模のマトリクスは作らない**。

| タイプ | 内容 | 受入れ条件の核 | ラベル / 課題タイプ |
|---|---|---|---|
| feature | 機能追加・仕様変更 | ユーザーに見える振る舞い | `type-feature` / Story |
| bugfix | 欠陥修正 | 再現手順で直っている + 回帰なし | `type-bugfix` / Bug |
| hotfix | 緊急の欠陥修正 | bugfix と同じ + 事後記録 | `type-hotfix` / Bug |
| refactoring | 外部振る舞い不変の内部改善 | 既存の振る舞いが変わらないこと | `type-refactoring` / Task |
| chore | 定型作業（依存更新、設定変更、軽微な文言） | 作業完了の機械的確認（CI green 等） | `type-chore` / Task |
| spike | 調査・検証（コードは成果物にしない） | 問いに答えが出ていること | `type-spike` / Task |

ラベルに `type-` prefix を付けるのは、プロジェクト固有の運用ラベルと機械的に区別するためである。

| フェーズ | feature | bugfix | refactoring | chore | spike |
|---|---|---|---|---|---|
| P1 着手 | ✓ | ✓ | ✓ | ✓ | ✓ |
| P2 要求整理 | ✓ | ✓（再現条件・回帰範囲） | ✓（不変条件・対象範囲） | 簡易 | ✓（問いの定義） |
| P2.5 プロトタイピング | UI 変更時 | − | − | − | − |
| P3 技術判断 | 判断駆動 | 判断駆動（稀） | 判断駆動 | − | 結論を ADR 化しうる |
| P4 設計 | 判断駆動 | − | 判断駆動 | − | − |
| P5 実装計画 | ✓ | 簡易（PR 説明 1 段落） | ✓ | − | − |
| P6 実装 | ✓ | ✓（再現テスト先行） | ✓（既存テスト green 維持） | ✓ | 使い捨てコード可（マージしない） |
| P7 検証・レビュー | ✓ | ✓ | ✓ | 簡易 | 報告のレビュー |
| P8 振り返り | 推奨 | 任意 | 任意 | − | 必須（学びが主成果物） |

hotfix は bugfix の緊急変形とする。ゲートを P6・P7 の最小構成に圧縮して先に直し、事後に P8 振り返りと記録（なぜ緊急対応したかの Intent Delta、必要なら ADR）を必須にする。事後記録漏れの専用監査は設けない（「Issue がなければリリースできない」社内ルールが入口で防ぐため）。

運用ルール:

- タイプが混在するIssue（機能追加 + ついでのリファクタリング等）は分割する（「変更を小さく刻む」の適用）。分割できない場合は重い方（feature）のルートに乗せる
- タイプはIssue 作成時に決める（`product-intent` が必須入力として確認する。迷う場合は AI が推測せず人間に確認する）
- タイプラベルのないIssue には着手しない。P1 着手で検出したらIssue入口へ差し戻す
- 着手後にタイプが誤りと判明した場合はラベルを修正し、P1 着手からルートを引き直す
- ルートはフェーズの実施有無を決めるだけで、成果物の深さは決めない。実施するフェーズの中で何をどこまで書くかは判断駆動の原則に従う
- 実施しないフェーズの責務が消えるわけではない。例えば chore で「守るべき振る舞い」に触れたなら、P7 検証・レビューの短期コンテキストの昇格ルールで設計書化する

### タイプ別Issueテンプレート（骨子）

feature は現行の Product Intent テンプレートをそのまま使う。他タイプは必須項目を絞った軽量テンプレートとし、詳細は `product-intent` 改修時に確定する。エンジニア起点のIssue（bugfix / chore / spike）は確認ループを最小にし、記入が数分で終わることを設計条件とする。

- **bugfix**: 再現手順、期待する挙動、実際の挙動、影響範囲（誰が・どこで）、回帰の懸念箇所
- **hotfix**: bugfix と同じ + 緊急性の根拠。Issue 作成が事後になることを許容する（先に直し、Issue と記録を事後に整える）
- **refactoring**: 動機（なぜ今やるか）、対象範囲、不変条件（守るべき振る舞い）、対象外
- **chore**: 作業内容、完了の確認方法
- **spike**: 答えるべき問い、背景、time-box、成果物の形（ADR / 報告メモ）

## フェーズ詳細

### P1 着手

- **entry**: 品質チェック済み GitHub Issue（Product Intent）
- **AI**: `workspace-hygiene` で作業ツリーを整理し topic branch を作成。Issue を読解し、自分の言葉で要求を言い直す。タイプラベル（`type-*`）からルートを特定し、適用するフェーズ構成を提示する（ラベルのないIssue は進めず、Issue入口へ差し戻す）。feature の場合は UI に見える変更を含むかを判断し、P2.5 プロトタイピングの実施要否を提案する
- **人間**: 言い直しのずれを修正し、ルートと P2.5 プロトタイピング 実施要否を承認する
- **議論の中心**: スコープの境界とリスク認識のすり合わせ
- **exit**: 人間が「AI の理解はずれていない」と確認し、ルートが合意されている
- **残すコンテキスト**: ルートと判断理由を 1〜2 行で Issue コメントへ

### P2 要求整理

- **entry**: ルート合意済み
- **AI**: Product Intent を開発で使える粒度へ具体化する。(1) 受入れ条件の曖昧・矛盾・漏れ（エッジケース）を指摘する。(2) 受入れ条件から導かれる技術品質条件（テスト範囲、性能、互換性、セキュリティ、観測性）を提案する。各受入れ条件・守るべき振る舞いをどのテストレベル（unit / integration / E2E）で検証するかを割り当て、各品質条件には検証手段タグ（CI / テスト / 手動）を付ける（P6・P7 のどちらが何を検証するかを機械的に決めるため）。観点は `requirements` skill の標準質問リストを走査し、該当する観点だけを測定可能な基準として書き、該当しない観点は「他は対象外」の 1 行に畳む。確定した受入れ条件・品質条件には ID（`AC-1`…、`QC-1`…）を振り、以後の実装計画・テスト・対応表・検証記録はこの ID で参照する（対応の完全性を機械チェック可能にするため）。各項目には信頼性マーカーを付ける — 🔵 根拠あり（出典明記）/ 🟡 妥当な推定 / 🔴 根拠なし・要確認。推測をマーカーなしで埋め込まない
- **人間**: 曖昧点に回答し、品質条件を承認する。プロダクト判断が必要な場合は PdM へ差し戻す（`product-intent` の確認ループへ戻る）
- **議論の中心**: 「完了とは何か」の確定。受入れ条件（ユーザー視点）と品質条件（技術視点）を分けて扱う
- **exit**: 全受入れ条件が「検証手順を書ける」粒度になっている。🟡・🔴 マーカーが残っていない（人間が確認して 🔵 にするか、対象外として畳む）
- **残すコンテキスト**: 確定版の受入れ条件・品質条件を Issue コメントへ（Issue本文は書き換えず Intent Delta として追記）
- **Intent**: Product Intent（入力）/ Intent Delta（追記）

### P2.5 プロトタイピング（UI に見える変更があるときのみ）

- **entry**: P2 要求整理の受入れ条件（暫定版でよい）。UI に見える変更を含まない場合はスキップ
- **AI**: `ui-prototyping` でプロトタイプを生成し、PdM と見た目・操作感をすり合わせる。確定後 `IMPLEMENTATION_PROMPT.md` を生成する
- **人間**: PdM がプロトタイプを承認する（「コレジャナイ」をここで潰す）
- **議論の中心**: 見た目・操作感・状態（初期・入力中・エラー・空など）の合意
- **exit**: 合意した UI の振る舞い・状態が受入れ条件へ反映されている（Issue へ Delta 追記）
- **残すコンテキスト**: プロトタイプ一式と `IMPLEMENTATION_PROMPT.md` → マージしない proto branch（GitHub Issue からリンク、短期層）。UI 受入れ条件の Delta → Issue コメント。合意した操作感・状態は P6 実装で Storybook・テストとして永続化し、プロトタイプ自体は使い捨てる
- **Intent**: Product Intent（Delta 追記）/ Design Intent（UI）

### P3 技術判断

- **外延（技術判断とは何か）**: 実装方式に関する判断。ライブラリ・ツールの選定に限らず、方針の決定（バリデーションを必須にする、リトライしない等）やデータ・仕様の扱い方の決定を含む。判定の目安は **採らなかった案を名指しできるか**。「何を作るか」のプロダクト判断は P2 要求整理 / escalation で扱う（この境界がフェーズ名の「技術」が担う情報）
- **entry**: 確定済み要求。選択肢のある技術判断が存在する場合のみ実施
- **AI**: 選択肢を整理し（選択肢が割れる、または影響が大きい場合は `multi-agent-discussion` を使用）、推奨案・理由・トレードオフ・採用しなかった選択肢を提示する。明確な優位案がなければ escalation する
- **人間**: 採用を判断する
- **議論の中心**: トレードオフの重み付け（何を優先し何を捨てるか）
- **exit**: 採用しなかった選択肢とその理由が ADR に書かれている
- **残すコンテキスト**: ADR → `docs/adr/{連番}-short-title.md`（イミュータブル。判断が変わったら新しい ADR で置き換える）
- **Intent**: Decision Intent

### P4 設計

- **entry**: 技術判断が確定（または判断不要と合意）
- **AI**: 目的・非目的、責務分割、データと状態の扱い、主要フロー、例外ケース、守るべき振る舞い、未確定事項を設計書にまとめる。各記述に信頼性マーカー（🔵/🟡/🔴）を付け、推定で書いた箇所を明示する。技術選定の理由は ADR へ寄せ、重複させない
- **人間**: 設計を承認する
- **議論の中心**: 責務の置き場所、既存設計との整合、例外ケースの扱い
- **exit**: 受入れ条件の各項目について「設計のどこで満たされるか」を対応付けられる
- **残すコンテキスト**: 設計書 → `docs/design/{連番}-short-title.md`
- **Intent**: Design Intent

### P5 実装計画

- **entry**: 設計承認済み（P3・P4 を実施しないルートでは P2 要求整理から直行）
- **AI**: 実装順序とその理由、変更対象と触らない範囲（それぞれ理由つき）、リスク、検証方針、人間確認ポイントを計画にまとめる。各ステップに TDD / DIRECT の区分を付け、DIRECT（テストで駆動できない作業: 環境構築・設定変更など）には実施手順と検証手順を必須にする
- **人間**: 計画を承認する
- **議論の中心**: 刻み方（1 ステップが PR レビュー可能な粒度か）と確認ポイントの配置
- **exit**: 各ステップが受入れ条件・設計のどの項目に対応するか（AC-ID 参照）と、TDD / DIRECT の区分が明示されている
- **残すコンテキスト**: 実装方針 → draft PR の説明文（使い捨て。判断理由のうち長期価値のあるものは P7 検証・レビューの短期コンテキストの昇格で記録化）
- **Intent**: Execution Intent

### P6 実装

- **entry**: 計画承認済み
- **AI**: `tdd-cycle` で実装する。テスト名・ケースを受入れ条件と対応付け、テストレベルは P2 要求整理で割り当てたものに従う。計画からの逸脱（スコープ変更、想定外の問題、新しい技術判断）が生じたら停止して人間へ確認する（現行 `escalation.md` を踏襲）
- **人間**: 確認ポイントで承認し、逸脱時の判断を行う
- **exit**: 全受入れ条件に対応するテストが green。品質条件（P2 要求整理で確定したもの）を満たしている
- **残すコンテキスト**: コードとテスト（テスト自体が Verification Intent の永続記録）。受入れ条件⇄テストの対応表を PR 説明へ
- **Intent**: Verification Intent

### P7 検証・レビュー

- **entry**: 実装完了
- **AI**: (0) `critical-gate`（フル版）を実行し、Critical 指摘を戻り先フェーズへ差し戻して解消する（通過まで人間レビューへ進まない）。(1) `review` skill で self-review。(2) 受入れ条件を 1 件ずつ検証し、手順と結果を記録する。(3) PR 説明を最終化する（ADR の設計意図・トレードオフ・影響範囲を含める — common.md の既存ルール）。(4) 短期コンテキストの昇格ルールに従い、長期層へ移すべき内容を提案する。(5) `context-snapshot` で、この開発単位の最新の意図と検証状態を人間向け HTML スナップショットとして生成し、受入れ判断の材料として提示する。スナップショットには信頼性マーカーの残存サマリー（未解決の 🟡・🔴 項目）を含め、受入れ判断で人間が見るべき注意点として明示する
- **人間**: ゲート通過後の PR をレビューし、受入れを判断し、短期コンテキストの昇格提案を承認し、マージする。マージ後のリリース検証もここに含める
- **exit**: critical-gate を通過している。全 AC の検証結果が AC-ID 付きで記録されている。未検証項目が残る場合は明示されている
- **残すコンテキスト**: ゲート判定・検証メモ → PR コメント。コンテキストスナップショット（HTML、生成日付き） → ワークスペースに使い捨て保存（要点は Issue コメント）。短期コンテキストから昇格した ADR・設計書（あれば）
- **Intent**: Verification Intent（記録）/ 全 Intent との整合確認

### P8 振り返り

- **entry**: マージ・リリース検証後
- **AI**: `retrospective`（AAR）を対話で実施する。①人間へ「想定と違った点」を先に問う（AI の観察を先出ししない）→ ②フェーズごとの意図と結果を並べる → ③ズレの原因が構造か個別事情かを切り分ける → ④沈黙の点検（記録されなかった判断・昇格されなかった知見・形式的に通しただけのゲート）→ ⑤合意した変更のみ記録。特に「意図が後工程へ伝わらなかった点」「認知負債になりかけた点」を抽出し、playbook の skill・テンプレートへの具体的な改善を提案する
- **人間**: 期待違反を先に提示し、原因の切り分けと改善の採否を判断する
- **exit**: 採用された改善が playbook への PR またはIssue になっている
- **残すコンテキスト**: 合意した変更 → playbook への PR・プロジェクトのルール/メモリ（振り返りの生ログは既定では Issue に残さない）
- **当事者性への対処**: AI が当事者であるプロセスを AI と振り返るため、進行役と被評価者が同一になる。(1) 人間の期待違反を入口に置き AI の観察を先出ししない (2) AI 自身の逸脱の申告を出力契約とする（「逸脱なし」には根拠必須）(3) 参照されなかった記述を申告させる（形骸化検出。AI にしか観測できない）(4) 作業文脈を渡さない独立チェックを併用する（毎回の契約チェック＝明示ルールとの照合は軽量モデルで足りる／判断を要する独立レビューは条件付きで強いモデル）。**契約チェックの通過を「独立レビュー通過」と報告しない**（機械的照合は判断を要する逸脱の不在を意味せず、偽の安心はゲート不在より悪い）
- **形式の選択理由**: KPT ではなく AAR を採る。AAR の中心問い（意図と結果の差分）が Intent 駆動の構造に一致し、因果を掘る導線が形式に内蔵されているため。KPT はスプリント単位のチーム活動用で、1 Issue 単位・人間 1 人 + AI の単発イベントには形が合わない。「うまくいったこと」の枠（Keep）は意図的に置かない（埋めるために書かれた誤った肯定を避ける）
- **Intent**: Learning Intent

## skill 分解案

| フェーズ | skill | 状態 |
|---|---|---|
| P1 着手 | orchestrator + 既存 `workspace-hygiene` | orchestrator は別途作成。最重要責務は進行管理ではなく、フェーズ exit 成果物の存在チェックによるゲート強制（ルート特定もここ） |
| Issue 作成 | 既存 `product-intent`（改修） | タイプを必須入力化し、タイプ別テンプレートで構造化する。課題タイプと `type-*` ラベルを設定してIssue を作成する |
| P2 要求整理 | `requirements`（新規） | 要求整理と品質条件の確定（AC/QC ID と信頼性マーカーを出力契約に含む） |
| P2.5 プロトタイピング | 既存 `ui-prototyping`（改修） | 入力を Product Intent / P2 要求整理 受入れ条件ベースへ変更。UI 合意を受入れ条件の Delta として Issue へ出力する契約と、`IMPLEMENTATION_PROMPT.md` を P5 実装計画の入力として渡す契約を追加 |
| P3 技術判断 | `adr`（新規、`dev/adr.md` を独立 skill 化） | `multi-agent-discussion` を内部で利用 |
| P4 設計 | `design-docs`（新規） | common.md が参照しているが実体がない欠落を埋める |
| P5 実装計画 | `implementation-plan`（新規） | Execution Intent の出力契約を持つ（ステップごとの TDD / DIRECT 区分を含む） |
| P6 実装 | 既存 `tdd-cycle`（改修） | 受入れ条件⇄テスト対応の出力契約を追加 |
| P7 検証・レビュー | 既存 `review`（改修） | 受入れ検証と短期コンテキストの昇格判定を契約に追加 |
| P7 検証・レビュー（P2/P5 軽量版） | `critical-gate`（新規、2026-07 追加） | 人間レビュー前の批判的チェックゲート。独立視点の agent が指摘の戻り先（P2/P3-P4/P6）を判定し、Critical 解消まで通過させない |
| P7 検証・レビュー | `context-snapshot`（新規） | Issue（Delta 適用後の意図）・ADR・設計書・受入れ条件⇄テスト対応・検証結果から、人間向け HTML スナップショットを生成する。論点 1 の「生成ビュー」の実体。P7 検証・レビュー 以外でも任意のタイミングで実行できる |
| P8 振り返り | 既存 `retrospective`（改修） | playbook 改善 PR への接続を契約に追加 |

`dev` は移行期間中、薄い入口（フェーズへの振り分けのみ）として残し、orchestrator 完成後に廃止または orchestrator に統合する。

## 論点（議論したい点）

叩き台作成者（AI）の意見を含む。賛否を議論したい。

1. **長期コンテキストに薄い可変レイヤが必要ではないか。** イミュータブルな ADR + Delta の積み上げは判断履歴の保全には優れるが、「いまの設計がどうなっているか」の把握には不向き（Delta が 20 個になった時点で順読みは破綻する）。意見: 判断記録（immutable）とは別に、living な現在仕様ドキュメント（例: `docs/spec/`、改訂時は旧版を archive）を持つべき。これは intent-driven-development.md の「1 つの文書へ集約しない」「上書きしない」方針と部分的に衝突するため、原則の例外として明文化が必要。**結論（2026-06-12）**: 人間が維持する living doc は持たない。現在状態はコード・テスト・不変記録からその場で生成する view として扱い、P7 検証・レビューの `context-snapshot`（HTML スナップショット）がその実体。スナップショットは再生成可能な使い捨てで、不変記録と食い違った場合は記録側を正とする。
2. **Intent Context ファイルは作らず、GitHub Issueを索引にすべきではないか。** intent-driven-development.md は repo 内に Intent Context ファイルを置く想定だが、「短期コンテキストは PR/Issue に残るだけでよい」という今回の方針と運用負荷の観点から、Issue に ADR・設計書・PR へのリンクを貼ることで索引の役割を果たす方が軽い。意見: repo 内 Intent Context ファイルは作らない。採用する場合 intent-driven-development.md の改訂が必要。**結論（2026-06-12）**: repo 内 Intent Context ファイルは作らず、GitHub Issueを索引にする（相互リンク規約を参照）。intent-driven-development.md の改訂は次のステップで行う。
3. **mode 判断の責務はどこに置くか。** 案 A: orchestrator（P1 着手）が判断する。案 B: `requirements`（P2 要求整理）で要求を具体化した後でないと規模は判断できないので P2 要求整理の出力とする。意見: 初期判断は P1 着手、P2 要求整理 完了時に再評価（mode の引き上げのみ許可、引き下げは人間判断）のハイブリッド。**結論（2026-06-12）**: mode（Lite / Standard / Full）という規模軸自体を廃止し、Issueタイプ別ルート方式へ置き換える（「タイプ別ルート」参照）。ルートは P1 着手で課題タイプから機械的に特定し、迷う場合は推測せず都度人間に確認する。深さの調整は判断駆動の成果物作成と escalation が担う。
4. **品質条件の標準セットを playbook 側に持つか。** P2 要求整理で毎回ゼロから品質条件を考えるのは不安定。意見: playbook に標準チェックリスト（テスト範囲、性能、互換性、セキュリティ、観測性）を置き、プロダクト repo 側で preset として上書き可能にする（stack 差分の preset/variant ルールと整合）。テストレベルの配分方針（ピラミッド型・トロフィー型など）も、比率の目標ではなく「どの種類の振る舞いをどのレベルで検証するか」の規則として preset に持たせる。**結論（2026-06-12）**: 採用。標準質問リスト（テスト範囲・レベル、性能、互換性、セキュリティ、観測性）を `requirements` skill 配下に置き、該当する観点だけを測定可能な基準として記載（該当しない観点は「他は対象外」の 1 行）、各品質条件に検証手段タグ（CI / テスト / 手動）を付ける。プロダクトごとの上書き preset は playbook 側の presets/ に置く（ui-prototyping と同じ機構）。

## 機械化候補（CI / hook）

以下は規律（AI が指示に従うこと）に頼らず機械的に強制できる項目の一覧。実装は各プロダクト repo の CI・hook で行うが、何を機械化すべきかの一覧は playbook を正とする。

- 着手時: タイプラベル（`type-*`）の存在、作業ツリーがクリーンであること
- 命名: branch 名・PR タイトルのIssue 番号形式、`docs/adr/`・`docs/design/` のファイル名形式と連番重複
- PR: 必須セクション（対応表・技術判断・昇格判定・未検証項目）の存在
- 対応の完全性: 全 AC-ID が対応表に出現し、対応するテストが存在すること
- マーカー残存: ゲート通過時、成果物に 🟡・🔴 の信頼性マーカーが残っていないこと（grep で検出可能）
- ゲート 1: テスト・lint・型・カバレッジ（既存 CI）

## 未確定事項

- 各プロダクト repo の `docs/` の現状構成（既存文書の命名との整合）
- orchestrator と各 phase skill の間の入出力形式（ルート・Issue 番号の受け渡し方法）
- `type-*` ラベルの GitHub Issue 側での運用整備（ラベルの事前登録、既存Issue への遡及付与の要否）
- playbook の配布・更新方式: 現状は各開発者のローカル clone をパス参照（README の導入手順）。版の固定と、CI からの参照可能性（機械化候補の実装に必要）を考えると、submodule 等による版管理の検討が必要。**当面の決定（2026-06-12）**: パス参照 + `git pull` 周知で運用する。問題が振り返りで顕在化した時点、または機械化候補を CI に実装する時点で submodule 等を再検討する
- リリース検証の具体的手順（プロダクトごとに異なるため preset 化の候補）
- プロジェクトレベルのコンテキストスナップショット（リッチ README / オンボーディングビュー）: main マージ後のプロジェクト全体の状態を人間向けに生成するビュー。新規参加者の最初の一読と、エンジニアの「いまどういう状況だっけ」に答えることが目的。Issue単位の `context-snapshot` の入力範囲を広げる形で実装できる見込み。中身は先に形を決めず、P8 振り返りで集まる「現状把握のために実際に出た質問」をもとに設計・ブラッシュアップする。実装は tsumiki の rev-*（既存コードからの文書逆生成）が参考になる。信頼性マーカーは過去Issue の集計ではなく、生成されるビュー自身の各記述に付与する（どの記述が ADR・設計書に根拠を持ち、どれがコードからの推定かを読者に示す）

## 次のステップ

論点 1〜4 は議論のうえ確定済み（各論点の「結論」を参照）。

1. `product-intent` をタイプ必須入力・タイプ別テンプレート・`type-*` ラベル付与へ改修する（後続のルーティングがすべてラベルに依存するため最初に行う）
2. `requirements` → `design-docs` → `implementation-plan` → `adr` → `context-snapshot` の順で新規 skill を作成する
3. `tdd-cycle` / `review` / `retrospective` / `ui-prototyping` に出力契約を追記する
4. `shared/rules/common.md` の Core Skills と Default Process（Lite / Standard / Full からタイプ別ルートへ）を更新する
5. intent-driven-development.md に必要な改訂（論点 1・2 の結論、命名規則の `{連番}-{Issue 番号}-short-title.md` 形式への変更）を Delta として追記する
